package org.example.remote;

import org.example.objects.VirtualMachine;

public class RemoteControl extends Remote{

    public RemoteControl(VirtualMachine virtualMachine) {
        super(virtualMachine);
    }
}
